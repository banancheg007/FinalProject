package ui.pageHelpers;

import ui.managers.AppManager;
import ui.managers.PageManager;

public class RequestsPagePageHelper extends BasePageHelper {
    public RequestsPagePageHelper(AppManager appManager, PageManager pageManager) {
        super(appManager, pageManager);
    }
}
