package ui.pageHelpers;

import ui.managers.AppManager;
import ui.managers.PageManager;

public class ServicesPageHelper extends BasePageHelper {

    public ServicesPageHelper(AppManager appManager, PageManager pageManager) {
        super(appManager, pageManager);
    }
}
