package ui.pageHelpers;

import ui.managers.AppManager;
import ui.managers.PageManager;

public class NewsPageHelper extends BasePageHelper {
    public NewsPageHelper(AppManager appManager, PageManager pageManager) {
        super(appManager, pageManager);
    }
}
